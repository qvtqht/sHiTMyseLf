SELECT
	item_score AS item_score,
	value AS item_url,
	item_title AS item_title,
	author_key AS author_key,
	item_attribute.file_hash AS file_hash
FROM
	item_attribute
		LEFT JOIN item_flat
			ON (item_flat.file_hash = item_attribute.file_hash)
WHERE
	item_attribute.attribute = 'url'


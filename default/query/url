SELECT
	value AS url,
	item_title,
	item_flat.file_hash AS file_hash
FROM
	item_attribute
	LEFT JOIN item_flat ON (item_attribute.file_hash = item_flat.file_hash)
WHERE
	attribute = 'url'
ORDER BY
	item_score DESC
LIMIT 50


SELECT
	item_title,
	item_flat.author_key AS author_id,
	add_timestamp,
	file_hash
FROM item_flat
LEFT JOIN author_score ON (item_flat.author_key = author_score.author_key)
WHERE
	author_score > 9000 AND
	item_score > 0 AND
	tags_list LIKE '%hastitle%'
ORDER BY add_timestamp DESC
LIMIT 25
